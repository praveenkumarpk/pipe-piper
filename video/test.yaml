label: HLS (Single Bitrate)

inputs:
  - name: input
    label: Input File
    type: string
    required: true

  - name: output
    label: Output Directory
    type: string
    required: true

tasks:

  - type: bash
    label: Create new directory
    script: |
      echo "HI"
      echo ${output}
      mkdir ${output}
